package us.richlandbombers.MusicSearch.bdb;

public class Interpreter
{
    //Because we have to be special and not use existing stuff.

    private static final String[] cmds =
    {
            "select",
            "find",
            "eval",
            "edit"
    };
}
